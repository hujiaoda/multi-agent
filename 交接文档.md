# 多智能体开发小队 —— 交接文档

> 给下一个 agent / 协作者的快速上手说明。详细待办见 `待做事项.txt`。

## 项目一句话

输入一句自然语言需求，四个 AI 角色（规划/编码/审查/测试）协作产出可运行代码：
需求不明确会主动追问澄清，审查/测试不通过会强制回炉重写，测试用真实运行（subprocess）验证。

## 架构

```
clarify(需求澄清, Qwen 结构化输出)
  ├─ 信息不足 → main.py 循环问用户（最多 MAX_CLARIFY=3 轮）
  └─ 信息足够 → plan(规划, web_search) → code(编码, 文件工具)
      → review(审查, 读文件) → test(测试, subprocess 真跑)
      review/test 不通过 → 条件边强制回 code（MAX_ATTEMPTS=5 硬切断）
```

核心设计：**文件系统是真相，State 是台账**。编码节点通过工具读写磁盘，
State 只记录 `files` / `entry_file` / 各节点产出。

## 文件清单

| 文件 | 职责 |
|---|---|
| `model.py` | DeepSeek 模型（timeout=120, max_tokens=4000, max_retries=2） |
| `tools.py` | web_search(@tool) + list/read/edit/write_file + run_python_file；`set_workspace` 路径兜底 |
| `react.py` | 通用 LLM+工具循环 `call_model`（含工具进度打印） |
| `structured.py` | Qwen 结构化输出 `ClarifyDecision`（需求澄清判定） |
| `prompt.py` | 各角色提示词（clarify/plan/code/review/test） |
| `state.py` | State 定义 |
| `graph.py` | StateGraph：节点 + 条件边 + strip_code_fences |
| `main.py` | 入口：多行输入、澄清循环、双模式流式、set_workspace |

## 当前状态（重要，已更新）

- **已成功跑通**：用户实测 2 个左右算法题（plan→code→review→test 全通过，结果准确）
- 早期失败都已修复并验证：
  - 贪吃蛇空测试假通过 → C 修复（内容检查 + ALL TESTS PASSED）
  - 三数之和 DeepSeek 超时 → 参数调优（timeout=120 / max_tokens=4000）
  - 大数乘法编码节点不写文件 → 路径兜底 + prompt 约束 + 进度打印
- 下一步：稳定复现成功运行 + 留存证据（work 产物 / 截图），补 README demo
- 路径问题已修：相对路径经 `set_workspace` 全部落进 `work/<时间戳>/`，不碰项目根目录。
- git：master 分支，最近提交 `5f261cd`；当前有 7 个文件改动未提交。

## 环境

- Python 3.13：`C:\Users\28401\AppData\Local\Programs\Python\Python313\python.exe`
- 关键依赖：langchain 1.3.14 / langgraph 1.2.10 / langchain-openai / langchain-deepseek / tavily-python
- 密钥在 `.env`（已被 .gitignore 排除，不上传）：DeepSeek / Qwen / Tavily / LangSmith
- 运行：在 `C:\Users\28401\multi-agent` 下 `python main.py`（用集成终端，别按 F5）

## 踩坑记录（本会话积累，别再踩）

1. **@tool 对象不能直接调用**（本版本 StructuredTool 无 __call__）→ 节点和 LLM 都要用的函数用普通函数 + `bind_tools`
2. **工具映射**：普通函数用 `f.__name__`，不是 `.name`
3. **Markdown 代码块**：模型会包 ```python → `strip_code_fences` 剥壳 + prompt 禁令双保险
4. **子进程中文 GBK**：`run_python_file` 里 `PYTHONUTF8=1` 强制 UTF-8
5. **空测试文件假通过**：已加内容检查（必须有 assert 或降级标记）+ ALL TESTS PASSED 完成标记
6. **编辑器保存冲突**：磁盘被改后，编辑器旧缓冲保存会覆盖 → 先"还原文件"或重开
7. **F5/Debug Console 中文隐形字**：渲染 bug，用集成终端跑
8. **DeepSeek 超时频繁**：网络慢是常态，参数已调（120s/4000tokens/2 重试），别再调小
9. **输入空行截断**：多行粘贴遇空行即断，残留行可能被 shell 执行（未修，TODO #4）
10. **编码节点"只说不写"**：核心未解问题，先取证再改（见"当前状态"）

## 用户背景与教学方式

- 用户是 LangChain/LangGraph 学习者，会 RAG/checkpointer/Store/create_agent，正在学业务 State、多节点图、消息管理
- 教学要求：先讲概念再给代码；一次只推一个点；讲清"这到底有啥用"；运行出错先诊断根因再修；默认用户"不会"
- 交流用中文；代码要能直接跑（Windows/PowerShell）
