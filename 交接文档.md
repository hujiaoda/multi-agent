# 多智能体代码开发小队 —— 交接文档

> 给下一个 agent 的完整上下文。待办清单见 `待做事项.txt`。

## 项目一句话

输入一句自然语言需求，四个 AI 角色（规划/编码/审查/测试）协作产出可运行代码：
需求不明确会主动追问澄清，审查/测试不通过会强制回炉重写，测试用真实运行（subprocess）验证，
LLM 写断言、机器判结果，不靠自评。

## 现状（已更新，准确版）

- **已实测跑通**：用户试过 2 个左右算法题，plan→code→review→test 全通过，结果准确
- 早期三个问题都已修复并验证：
  - 贪吃蛇空测试假通过 → C 检查（测试文件必须有 assert 或降级标记）+ ALL TESTS PASSED 完成标记
  - 三数之和 DeepSeek 超时崩溃 → model.py 参数调优（timeout=120 / max_tokens=4000 / max_retries=2）
  - 大数乘法编码节点不写文件（work 目录空）→ 路径兜底 set_workspace + codeprompt 第6条 + 进度打印
- 用户目前倾向：**暂时不再跑系统**，想先做后面的待办（具体哪条还没定，需要先和他确认）

## 架构

```
clarify(需求澄清, Qwen 结构化输出)
  ├─ 信息不足 → main.py 循环问用户（最多 MAX_CLARIFY=3 轮）
  └─ 信息足够 → plan(规划, web_search 工具循环) → code(编码, 文件工具)
      → review(审查, 读文件) → test(测试, subprocess 真跑)
      review/test 不通过 → 条件边强制回 code（MAX_ATTEMPTS=5 硬切断）
```

核心设计：**文件系统是真相，State 是台账**。编码节点通过工具读写磁盘，
State 只记录 `files` / `entry_file` / 各节点产出。

## 文件清单

| 文件 | 职责 |
|---|---|
| `model.py` | DeepSeek 模型（timeout=120, max_tokens=4000, max_retries=2） |
| `tools.py` | web_search(@tool) + list/read/edit/write_file + run_python_file；`set_workspace` 路径兜底 |
| `react.py` | 通用 LLM+工具循环 `call_model`（含 `[工具]` 进度打印） |
| `structured.py` | Qwen 结构化输出 `ClarifyDecision`（需求澄清判定） |
| `prompt.py` | 各角色提示词（clarify/plan/code/review/test） |
| `state.py` | State 定义 |
| `graph.py` | StateGraph：节点 + 条件边 + strip_code_fences |
| `main.py` | 入口：多行输入、澄清循环、双模式流式、set_workspace |

## 环境

- Python 3.13：`C:\Users\28401\AppData\Local\Programs\Python\Python313\python.exe`
- 依赖：langchain 1.3.14 / langgraph 1.2.10 / langchain-openai / langchain-deepseek / tavily-python
- 密钥在 `.env`（gitignore 排除）：DeepSeek / Qwen / Tavily / LangSmith
- 运行：`C:\Users\28401\multi-agent` 下 `python main.py`（**用集成终端，别按 F5**）
- git：master 分支，最新提交 `27221c0`；GitHub 仓库已建但**最新改动未推送**

## 下一步方向（详见 待做事项.txt）

- 技术债：#5 路径沙箱（绝对路径越界拦截）、#6 输入多行截断、#7 推送 GitHub
- 简历加分：#8 记忆层（Store + RAG 经验库）、#9 interrupt() + checkpointer 人机交互升级
- 泛化：#12 前端（FastAPI + SSE）、#13 多语言/任意文件
- 面试准备：8 个必问答 + 项目一句话定位（中/英）
- **第一件事**：和用户确认接下来做哪条（推荐先 #5/#6/#7），讨论清楚再动手

## 踩坑速查（别再踩）

1. `@tool` 对象不能直接调用（本版本 StructuredTool 无 __call__）→ 节点/LLM 都要用的函数用普通函数 + `bind_tools`
2. 工具映射用 `f.__name__`，不是 `.name`
3. 模型会包 Markdown 代码块 → `strip_code_fences` + prompt 禁令双保险
4. 子进程中文 GBK 崩溃 → `run_python_file` 里 `PYTHONUTF8=1`
5. 空测试文件假通过 → 内容检查 + ALL TESTS PASSED
6. 编辑器旧缓冲会覆盖磁盘改动 → 先"还原文件"或关掉不保存重开
7. F5/Debug Console 中文隐形字 → 用集成终端跑
8. DeepSeek 网络慢是常态，超时参数已调好，别改回去
9. 多行输入遇空行截断 + 残留行可能被 shell 执行（未修，TODO #6）

## 用户背景与教学方式

- LangChain/LangGraph 学习者，会 RAG/checkpointer/Store/create_agent；目标是实习项目
- 教学要求：先讲概念再给代码；一次只推一个点；讲清"这到底有啥用"；运行出错先诊断根因再修；默认用户"不会"
- 用中文交流；代码要能直接跑（Windows/PowerShell）
