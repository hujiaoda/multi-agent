待做事项清单（目标：可写进简历、面试能讲清楚）
================================================

🎯 实习就绪第一优先（已能跑通，接下来是稳定复现 + 留证据）
1. 稳定复现成功运行 + 留存证据
   - 现状：已实测跑通 2 个左右算法题（结果准确），早期失败均已修复
   - 再做 2~3 次成功运行，确认不是碰运气：
     [工具] 进度输出正常 / work 目录有 main.py 和 test_target.py / 测试真跑真通过
   - 把成功的 work 产物（main.py / test_target.py）整理进 demo/，截图存档
2. 若复现"编码节点只说不写"：加"无产出反馈重跑"机制
   - 编码节点跑完 files 为空 → 反馈"上一轮没写任何文件，必须真正调用 write_file"→ 重写
   - 不要硬编码 main.py 兜底（交付物可能是 txt/C++/多文件，兜底无效）
3. 整理 2~3 个演示需求并各跑通一次：
   - 函数级：写一个斐波那契函数
   - CLI 级：写一个命令行计算器
   - 多文件级：写一个带模块拆分的排序工具
4. 给 README 补 2~3 张真实运行截图 + 成功产物示例（work 目录的 target/test 文件）

🔴 技术债（跑通之后）
5. 路径安全沙箱：绝对路径越界拦截（相对路径已兜底，绝对路径还没拦）
6. 输入多行截断修复：空行当结束符 → 改"两个空行"或"end 行"（demo 翻车点）
7. 提交并推送 GitHub（当前 f93ba9e 已提交未推送）

🟡 简历加分
8. 记忆层 E：Store 存历史项目（需求/代码/踩坑）+ embedding 检索 + 注入 prompt
9. interrupt() + checkpointer：人机交互升级（断点续跑，不重跑图）
10. entry_file 支持 LLM 指定（现在硬编码 main.py）
11. memory.py 空文件：删掉或留作记忆层占位

⚪ 最后做（泛化）
12. 前端：FastAPI + 单页 HTML，SSE 流式输出
13. 多语言/任意文件：按语言选运行命令，测试降级为编译+冒烟
14. 测试质量再加深：更多边界断言、覆盖率思路

🎤 面试准备（写简历前必做）
- 准备 5~8 个必问答：
  1. 为什么 State 是角色间数据契约？谁写谁读？
  2. 反思循环怎么实现？为什么用条件边不用 prompt？
  3. 怎么验证代码正确？为什么"LLM 写断言、机器判结果"？
  4. 验证的边界是什么？（交互程序只能冒烟）
  5. LLM 输出怎么消毒？（prompt 禁令 + strip_code_fences）
  6. 工具为什么用普通函数 + bind_tools？（@tool 不能直接调用）
  7. 遇到最大的坑是什么？怎么解决的？（Markdown 代码块 / 空测试假通过 / 编码节点不写文件）
  8. 项目最大的不足是什么？下一步怎么改进？
- 项目一句话定位（中文/英文各一版，简历和自我介绍用）

⚠️ 操作习惯（容易踩）
- 跑程序用 VS Code 集成终端（Ctrl+`），别按 F5：Debug Console 渲染中文有"隐形字" bug
- 我改完文件后，编辑器旧缓冲会覆盖磁盘改动：先"还原文件"或关掉不保存重开
- 发消息给我前先 Ctrl+S，否则我看不到你的最新修改
- DeepSeek 网络慢是常态，超时/重试参数已在 model.py 调过，别乱改回去

✅ 已完成
- 四角色骨架 + 反思循环（条件边 + MAX_ATTEMPTS 硬切断）
- 实测跑通 2 个左右算法题（plan→code→review→test 全通过，结果准确）
- work/<时间戳>/ 独立目录 + 路径兜底（相对路径落进运行目录）
- 流式输出（updates + messages 双模式 + JSON/工具片段过滤 + 警告抑制）
- 需求澄清：Qwen 结构化输出 + 人机交互（第一版）
- C：防假通过 + ALL TESTS PASSED 完成标记
- 文件操作工具 + 工具进度打印
- DeepSeek 超时调优（timeout=120 / max_tokens=4000 / max_retries=2）
- README + requirements.txt + .env.example + GitHub 仓库建立

面试复习点（可讲的设计决策）
- State = 数据契约；条件边反思循环；MAX_ATTEMPTS 硬切断
- 验证哲学：LLM 写断言，机器判结果；显式降级，不假装通过
- LLM 输出消毒：prompt 禁令 + strip_code_fences 双保险
- 文件系统是真相，State 是台账；按"谁用"决定工具形态
- 多智能体 + 按需人机交互（澄清节点），不是全程对话驱动
