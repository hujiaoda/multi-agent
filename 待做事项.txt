待做事项清单（按优先级）
========================

✅ 已完成
- 四角色骨架 + 反思循环（条件边 + MAX_ATTEMPTS 硬切断）
- work/<时间戳>/ 每次运行独立目录（可追溯、不冲突）
- 路径兜底：set_workspace 让文件工具的相对路径全部落进本次运行目录
- 流式输出（updates + messages 双模式）
- 流式输出清理：过滤 JSON/工具调用片段 + 抑制 Pydantic 警告
- 工具进度打印：react.py 每次执行工具前输出 [工具] xxx(...)
- 需求澄清：Qwen 结构化输出 + 人机交互（第一版：重跑式）
- C：防假通过（空测试判失败）+ ALL TESTS PASSED 完成标记
- 文件操作：list_files / read_file / edit_file / write_file（工具已接，待真实验证）
- DeepSeek 参数调优：timeout=120 / max_tokens=4000 / max_retries=2
- README + requirements.txt + .env.example + 上传 GitHub

🔴 优先做
1. 验证文件操作真的跑通：跑一次"大数乘法"（或"给 main.py 加个函数"）
   - 上次运行 work 目录为空 = 编码节点没真正写文件（只说不做）
   - 已加：进度打印 / 路径兜底 / prompt 第6条（空目录直接写）/ max_turns=8
   - 但修复后还没跑过：先跑一次，看 [工具] 输出，确认模型到底调没调 write_file
2. 若确认"模型只说不写"：加"无产出反馈重跑"机制
   - 编码节点跑完 files 为空 → 反馈"上一轮没写任何文件，必须真正调用 write_file"→ 下一轮重写
   - 不要硬编码 main.py 兜底（交付物可能是 txt/C++/多文件，兜底无效）
3. 路径安全沙箱：绝对路径越界拦截（现在相对路径已兜底，绝对路径还没拦）
4. 输入多行截断修复：空行当结束符 → 改"两个空行"或"end 行"
   - 顺带解决残留行被 shell 执行的风险
5. 提交并推送最新改动到 GitHub（当前 7 个文件未提交）

🟡 之后做
6. 记忆层 E：Store 存历史项目（需求/代码/踩坑）+ embedding 检索 + 注入 prompt
7. interrupt() + checkpointer：人机交互升级（断点续跑，不重跑图）
8. entry_file 支持 LLM 指定（现在硬编码 main.py）
9. demo 截图 + README 补图（可选）
10. memory.py 空文件：删掉或留作记忆层占位

⚪ 最后做（泛化）
11. 前端：FastAPI + 单页 HTML，SSE 流式输出
12. 多语言/任意文件：按语言选运行命令，测试降级为编译+冒烟
13. 测试质量再加深：更多边界断言、覆盖率思路

⚠️ 操作习惯（容易踩）
- 跑程序用 VS Code 集成终端（Ctrl+`），别按 F5：Debug Console 渲染中文有"隐形字" bug
- 我改完文件后，编辑器旧缓冲会覆盖磁盘改动：先"还原文件"或关掉不保存重开
- 发消息给我前先 Ctrl+S，否则我看不到你的最新修改
- DeepSeek 网络慢是常态，超时/重试参数已在 model.py 调过，别乱改回去

面试复习点（可讲的设计决策）
- State = 角色间数据契约；条件边反思循环；MAX_ATTEMPTS 硬切断
- 验证哲学：LLM 写断言，机器判结果；显式降级，不假装通过
- LLM 输出消毒：prompt 禁令 + strip_code_fences 双保险
- 文件系统是真相，State 是台账；按"谁用"决定工具形态
- 多智能体 + 按需人机交互（澄清节点），不是全程对话驱动

面试复习点（可讲的设计决策）
- State = 角色间数据契约；条件边反思循环；MAX_ATTEMPTS 硬切断
- 验证哲学：LLM 写断言，机器判结果；显式降级，不假装通过
- LLM 输出消毒：prompt 禁令 + strip_code_fences 双保险
- 文件系统是真相，State 是台账；按"谁用"决定工具形态
